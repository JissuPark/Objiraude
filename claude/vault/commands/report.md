Sync today's (or `week`) work. Follow the `sync-report` skill.

- Task-doc `## Progress` rollup is automatic (in-vault, cumulative bullets).
- Jira status transitions, Confluence promotion, and company-repo promotion: show what goes where, confirm, then run.
- Don't post Jira comments/worklogs directly — point out the targets and hand off to `/jira-sync`.
