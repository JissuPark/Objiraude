Draft today's daily `## Tasks`. Follow the `daily-plan` skill.

- Pull my open Jira tasks and expand each into its open subtasks (`- [ ] [task] subtask (KEY)`).
- Carry over yesterday's unfinished lines. Jira read-only; subtask creation is confirmed first.
