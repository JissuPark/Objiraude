Organize the current conversation into a `<type>` Confluence draft, or update an existing document on the same topic (`$ARGUMENTS` = `<type> [title/topic]`; ask if type is missing). Follow the `confluence-draft` skill.

- Drafts go to the personal space only. Preview, confirm, then publish (no unattended publishing).
- body types (plan/spec/design) update the page body; thread types (work log/discussion) append comments.
- After saving, note that "/compact is now safe".
