Record the work you just did into the vault's today daily `## Tasks` (vault = `<VAULT_PATH>`, `$ARGUMENTS` = summary). Follow the `daily-log` skill.

- Route by the git branch `<PROJECT_KEY>-####` from any repo (else keyword match).
- One checkbox line (`[tag] summary (key) ⏰ HH:MM ✅ today`) + an optional `Problem/Fix/Result(links)` block.
- Put summaries & links only into the vault — never source code.
